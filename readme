Create a basic web server using Python's built-in http.server module. This server will serve static files (such as HTML, CSS, and images) from a specified directory and handle HTTP GET requests from clients.
